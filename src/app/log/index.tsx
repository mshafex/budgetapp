// Placeholder route — replaced by the LOGGING Phase-3 worker.
import { ActivityIndicator, View } from 'react-native';

export default function Log() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <ActivityIndicator />
    </View>
  );
}
